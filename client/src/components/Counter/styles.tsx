import { styled } from "styled-components";

export const Counter = styled.div`
  display: inline-flex;
  width: 30px;
  justify-content: center;
  span{
    all: unset;
    font-size: inherit;
    color: inherit;
    display: block;
  }

`